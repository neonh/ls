## Simple realization of "ls" utility
List information about the FILEs (the current directory by default).

### Usage
```
./ls [OPTION]... [FILE]...
```

### Options
```
  -a                         do not ignore entries starting with .
  -l                         use a long listing format
```
